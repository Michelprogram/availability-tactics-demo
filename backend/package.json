{
  "name": "availability-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev:primary": "cross-env ROLE=primary PORT=3001 node server.js",
    "dev:spare": "cross-env ROLE=spare PORT=3002 node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
